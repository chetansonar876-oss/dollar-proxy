{
  "name": "bullion-proxy-server",
  "version": "1.0.0",
  "description": "Proxy + Rate server for bullion rates with smart keep-alive",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "http-proxy": "^1.18.1",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
